import React from 'react';
import { OverlayView, OVERLAY_MOUSE_TARGET } from '@react-google-maps/api';

function RequestMarker(position, price, handleProfileClick, index) {
    console.log(price)
    return (
        <>
        </>
    )
}

export default RequestMarker
